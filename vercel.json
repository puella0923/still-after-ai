{
  "buildCommand": "npx expo export --platform web && node scripts/inject-seo.js",
  "outputDirectory": "dist",
  "installCommand": "npm install",
  "framework": null,
  "rewrites": [
    { "source": "/((?!_expo|assets|favicon\\.png).*)", "destination": "/index.html" }
  ]
}
